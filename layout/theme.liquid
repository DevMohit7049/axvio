<!DOCTYPE html>
<html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device.width , initial-scale=1.0">
      <link rel="stylesheet" href="{{ 'style.css' | asset_url }}">
      <link rel="icon" href="{{ 'three.ico' | asset_url }}">
      <title>Apple</title>
      {{ content_for_header }}
    </head>
    <body>
     {% section 'header' %}
      <main id="main">
        {{ content_for_layout }}  
      </main>
    </body>
</html>